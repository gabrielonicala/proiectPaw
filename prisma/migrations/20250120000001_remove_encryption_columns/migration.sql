-- Remove encryption columns migration
-- This migration was likely already applied manually or the columns were never added
-- Adding empty migration to maintain migration history

-- No changes needed - columns were removed in a previous update

